import {tw} from '@src/libs/tailwind';
import React from 'react';
import {Text, View} from 'react-native';

export const SurveyResultListScreen = () => {
  return (
    <View style={tw`h-full bg-white`}>
      <Text>SurveyResultListScreen</Text>
    </View>
  );
};
